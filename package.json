{
  "name": "jnu_web",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "concurrently -n server,web,admin -c cyan,green,magenta \"pnpm -C server dev\" \"pnpm -C web dev\" \"pnpm -C admin dev\"",
    "build": "pnpm -C web build && pnpm -C admin build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
