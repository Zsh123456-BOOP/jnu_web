import * as modules from './modules/modules';
import * as contents from './modules/contents';
import * as members from './modules/members';
import * as settings from './modules/settings';

const api = {
  modules,
  contents,
  members,
  settings
};

export { modules, contents, members, settings };
export default api;
